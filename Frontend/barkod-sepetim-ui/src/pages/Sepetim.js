import React from 'react';
import { Typography } from '@mui/material';

const Sepetim = () => {
    return (
        <div>
            <Typography variant="h4">Sepetim</Typography>
            <Typography>Alışveriş sepeti içeriği burada görünecek.</Typography>
        </div>
    );
};

export default Sepetim;